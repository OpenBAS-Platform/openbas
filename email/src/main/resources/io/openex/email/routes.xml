<routes xmlns="http://camel.apache.org/schema/spring">
    <route id="openex-email">
        <from uri="direct:email"/>
        <setBody>
            <simple>Hello email test: ${body[exercise_id]}</simple>
        </setBody>
        <to uri="stream:out"/>
    </route>
</routes>