<routes xmlns="http://camel.apache.org/schema/spring">
    <route>
        <from uri="direct:email" id="openex-email"/>
        <setBody>
            <simple>Hello email: ${body[exercise_id]}</simple>
        </setBody>
        <to uri="stream:out"/>
    </route>
</routes>