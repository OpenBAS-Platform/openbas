<routes xmlns="http://camel.apache.org/schema/spring">
    <route id="openex-sms">
        <from uri="direct:sms"/>
        <setBody>
            <simple>Hello SMS: ${body[exercise_id]}/${threadName}</simple>
        </setBody>
        <to uri="stream:out"/>
    </route>
</routes>