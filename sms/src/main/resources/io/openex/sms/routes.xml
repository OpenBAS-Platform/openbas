<routes xmlns="http://camel.apache.org/schema/spring">
    <route id="openex-sms">
        <from uri="direct:sms"/>
        <setBody>
            <simple>Hello SMS: ${body[context][id]}/${threadName}</simple>
        </setBody>
        <to uri="stream:out"/>
    </route>
</routes>