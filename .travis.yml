sudo: required
dist: trusty
language: node_js

node_js:
- 6

cache:
  directories:
  - docs/vendor/bundle
  - node_modules
  - $HOME/.yarn-cache

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y -qq yarn

install:
  - yarn install

script:
  - npm run build

notifications:
  email: contact@luatix.org