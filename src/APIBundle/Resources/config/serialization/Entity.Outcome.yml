APIBundle\Entity\Outcome:
    attributes:
        outcome_id:
            groups: ['outcome', 'incident']
        outcome_incident:
            groups: ['outcome']
        outcome_comment:
            groups: ['outcome', 'incident']
        outcome_result:
            groups: ['outcome', 'incident']