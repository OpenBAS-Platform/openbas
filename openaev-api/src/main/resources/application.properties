# Global configuration
# suppress inspection "SpringBootApplicationProperties"
info.app.name=@project.description@
# suppress inspection "SpringBootApplicationProperties"
info.app.version=@project.version@

# OpenAEV General Configuration
openaev.name=@project.name@
openaev.version=@project.version@
openaev.base-url=http://localhost:8080
openaev.application-license=
# Run mode for platform background processing:
# - normal: default mode, all background jobs are enabled
# - safe: startup degraded mode, Quartz background jobs are disabled
openaev.run-mode=normal
# openaev.logout-success-url=/
# Setting up the frontend url if frontend and backend are on a different server
# openaev.frontend-url=/
openaev.cookie-name=openaev_token
openaev.cookie-secure=false
openaev.cookie-duration=P1D
openaev.unsecured-certificate=false
openaev.with-proxy=false
openaev.extra-trusted-certs-dir=
#openaev.admin.email=admin@openaev.io # mandatory
#openaev.admin.password=ChangeMe # mandatory
#openaev.admin.token=ChangeMe # mandatory
#openaev.admin.encryption_key=ChangeMe #mandatory
#openaev.admin.encryption_salt=ChangeMe #mandatory - should be at least 8 bytes long
openaev.starterpack.enabled=true

# Telemetry deployment tags (optional): comma-separated freeform tags (e.g. saas,eu-west)
# attached to the anonymous usage telemetry as a resource attribute so adoption analytics
# can slice metrics by deployment dimension. Normalized (trim, lowercase, dedupe, sort).
telemetry.tags=

########################
# RUNTIME DEPENDENCIES #
########################
# PostgreSQL, MinIO and RabbitMQ must be configured (and running) for OpenAEV to work properly

### POSTGRESQL Configuration
### see also: https://docs.openaev.io/latest/deployment/configuration/#postgresql
# connection URL to the PostgreSQL server
# example: jdbc:postgresql://localhost:5432
spring.datasource.url=jdbc:postgresql://localhost:5432/openaev
# Username: should be identical to the username set on the database instance
#spring.datasource.username=copilot
# Password: the password for the username set above
#spring.datasource.password=copilot_dev
spring.datasource.username=openaev
spring.datasource.password=openaev
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_size=30
# Batch lazy/eager association loading (IN (?, ?, ...) instead of one SELECT per entity).
# The model has many EAGER collections without @Fetch(SUBSELECT); this caps the N+1 cost.
spring.jpa.properties.hibernate.default_batch_fetch_size=50
spring.datasource.hikari.maximum-pool-size=20
# Log the acquisition stack trace of any thread holding a connection for more than 30s ("Apparent
# connection leak detected", WARN on com.zaxxer.hikari — visible with production log levels).
# Together with io.openaev.diagnostics.PoolExhaustionWatchdog (ERROR + thread dump on sustained
# saturation), this names the code paths pinning connections whenever the pool gets exhausted.
# The connection is NOT killed: legit long holders (bulk indexing, migrations) just log evidence.
spring.datasource.hikari.leak-detection-threshold=30000
spring.datasource.hikari.data-source-properties.cachePrepStmts=true
spring.datasource.hikari.data-source-properties.prepStmtCacheSize=250
spring.datasource.hikari.data-source-properties.prepStmtCacheSqlLimit=2048
spring.datasource.hikari.data-source-properties.useServerPrepStmts=true

### ENGINE Configuration
# selector can be elk or opensearch
engine.engine-aws-mode=no
engine.engine-selector=elk
engine.index-prefix=openaev
engine.index-suffix=-000001
engine.url=http://localhost:9200
# How many models may run their indexing sync concurrently (default 3). Each sync pass holds a
# database connection for its whole fetch query; during a full reindex those queries are heavy,
# so a higher value trades platform responsiveness for reindexing throughput.
#engine.indexing-max-concurrent-models=3
# Threshold in milliseconds after which a model synchronisation is declared misfired and is
# rescheduled (default 120000). Avoids stacking repeated sync jobs for the same model when the
# engine sync thread pool is temporarily exhausted.
#engine.indexing-misfire-threshold-ms=120000
# Engine client timeouts and connection cap (defaults: 5s connect, 60s socket, 40 connections).
#engine.connect-timeout-ms=5000
#engine.socket-timeout-ms=60000
#engine.max-connections=40
# Indexing cursor grace window in seconds (default 60). The persisted cursor never gets closer to
# wall-clock than this window, so rows committed late by long transactions (whose updated_at was
# assigned at flush time) are still picked up. Must exceed the longest expected write transaction.
#engine.indexing-grace-window-seconds=60

### MINIO Configuration
### see also: https://docs.openaev.io/latest/deployment/configuration/#s3-bucket
# hostname of the S3 bucket service
minio.endpoint=localhost
# Port of the MinIO server
minio.port=9000
# Name of the bucket; the default is fine
minio.bucket=openaev
# key and secret for accessing the bucket
minio.access-key=<key>
minio.access-secret=<secret>
# S3 configuration
openaev.s3.use-aws-role=false
openaev.s3.sts-endpoint=

# Database auto-generated per branch
openaev.dev.auto-start-database=false
# If you want to set a static port. Otherwise, you can comment it
openaev.dev.database-port=5432
# Container runtime: auto-detected (podman first, then docker) or force with:
# openaev.dev.container-runtime=docker

### RABBITMQ settings
### see also: https://docs.openaev.io/latest/deployment/configuration/#rabbitmq
# hostname and port of the RabbitMQ server
openaev.rabbitmq.hostname=localhost
openaev.rabbitmq.port=5672
openaev.rabbitmq.prefix=openaev
openaev.rabbitmq.user=guest
openaev.rabbitmq.pass=guest
openaev.rabbitmq.vhost=/
# enable TLS for encrypted connections
openaev.rabbitmq.ssl=false
openaev.rabbitmq.management-port=15672
# RabbitMQ queue type ("classic" or "quorum")
openaev.rabbitmq.queue-type=classic
# Whether or not the calls to the management plugin of rabbitmq can be insecure
openaev.rabbitmq.management-insecure=true
# Hard cap on a single publish, so a broker that stops answering cannot hold a transaction open
openaev.rabbitmq.publish-timeout-ms=30000
# if the SSL key is set to true and Insecure Management to false,
# fill in the following settings to enable access
openaev.rabbitmq.trust-store-password=<trust-store-password>
openaev.rabbitmq.trust.store=<file:/path/to/client-store.p12>

openaev.queue-config.inject-trace.publisher-number=2
openaev.queue-config.inject-trace.consumer-number=8
openaev.queue-config.inject-trace.worker-number=8
openaev.queue-config.inject-trace.worker-frequency=10000
openaev.queue-config.inject-trace.queue-name=inject-trace
openaev.queue-config.inject-trace.max-size=200
openaev.queue-config.inject-trace.consumer-qos=1000
openaev.queue-config.inject-trace.publisher-qos=0

# Workflow engine queues
openaev.queue-config.workflows-ready.publisher-number=1
openaev.queue-config.workflows-ready.consumer-number=1
openaev.queue-config.workflows-ready.worker-number=1
openaev.queue-config.workflows-ready.worker-frequency=10000
openaev.queue-config.workflows-ready.queue-name=workflows-ready
openaev.queue-config.workflows-ready.max-size=200
openaev.queue-config.workflows-ready.consumer-qos=1000
openaev.queue-config.workflows-ready.publisher-qos=0

openaev.queue-config.workflows-update.publisher-number=1
openaev.queue-config.workflows-update.consumer-number=1
openaev.queue-config.workflows-update.worker-number=1
openaev.queue-config.workflows-update.worker-frequency=10000
openaev.queue-config.workflows-update.queue-name=workflows-update
openaev.queue-config.workflows-update.max-size=200
openaev.queue-config.workflows-update.consumer-qos=1000
openaev.queue-config.workflows-update.publisher-qos=0

openaev.cron.config.steps.delay.queue.polling.interval=10000

# Reporting module: server-side rendering (Playwright + in-container headless Chromium)
## Hard cap on a single render (navigation + readiness wait + capture), in seconds
openaev.reporting.render-timeout-seconds=90
## Maximum simultaneous headless browser contexts (bounds Chromium memory usage)
openaev.reporting.max-concurrent-renders=2
## Base URL the headless browser uses to reach the platform itself. Empty (default) uses
## loopback - http(s)://localhost:{server.port}{context-path} - which avoids any dependency
## on external DNS, reverse proxies or TLS termination. Override only when loopback does not
## serve the SPA.
openaev.reporting.render-base-url=

# Web server configuration
server.address=0.0.0.0
server.port=8080
server.servlet.context-path=/

# Session management
## Sessions are persisted in PostgreSQL (Spring Session JDBC) and survive platform restarts.
## Rolling idle timeout: every authenticated request extends the session by this duration.
server.servlet.session.timeout=1440m
spring.session.timeout=${server.servlet.session.timeout}
## Session tables are created by a Flyway migration, never by Spring Session itself.
spring.session.jdbc.initialize-schema=never
## Built-in cleanup is disabled: the SessionManager audit-aware cleanup job replaces it.
spring.session.jdbc.cleanup-cron=-
## Idle timeout surfaced to the frontend lock screen (0 = disabled). Must be lower than the
## session timeout to be useful. Supports duration syntax (e.g. 30m).
openaev.session-idle-timeout=0
## When true, the session cookie has no Expires attribute and dies when the browser closes
## (server-side timeout still applies). When false, the cookie persists for the session timeout
## and is re-issued on every request (rolling), so browser-side expiration slides with activity:
## a user navigating within the timeout window is never logged out.
openaev.session-cookie=false
## SameSite attribute of the session cookie. Blank (default) OMITS the attribute, which keeps the
## cookie flowing on the cross-site SSO callback (SAML ACS POST / OIDC form_post) - an explicit
## "Lax" would drop it and break SSO (redirect to /login?error). HTTPS SSO deployments may set this
## to "None" (which forces Secure) for a stricter, spec-compliant cross-site session cookie.
## Accepted values: None, Lax, Strict, or blank to omit.
openaev.session-cookie-same-site=
server.ssl.enabled=false
server.http2.enabled=true
server.ssl.key-store-type=PKCS12
server.ssl.key-store=classpath:localhost.p12
server.ssl.key-store-password=admin
server.ssl.key-alias=localhost
server.compression.enabled=true

# Authenticators
## Local
openaev.auth-local-enable=true
## Oauth
openaev.auth-openid-enable=false
## Configuration examples
### Auth0
# spring.security.oauth2.client.provider.{registrationId}.issuer-uri=https://auth.auth0.io/
# spring.security.oauth2.client.registration.{registrationId}.client-name=Login with auth0
# spring.security.oauth2.client.registration.{registrationId}.client-id=
# spring.security.oauth2.client.registration.{registrationId}.client-secret=
# spring.security.oauth2.client.registration.{registrationId}.redirect-uri=${openaev.base-url}/login/oauth2/code/{registrationId}
# spring.security.oauth2.client.registration.{registrationId}.scope=openid,profile,email
### GitHub / Facebook or any other pre handled OAuth2
# spring.security.oauth2.client.registration.{registrationId}.client-name=Login with Github
# spring.security.oauth2.client.registration.{registrationId}.client-id=
# spring.security.oauth2.client.registration.{registrationId}.client-secret=

## Saml2
openaev.auth-saml2-enable=false
## Configuration examples
### Microsoft / Okta or any other saml2 providers
# spring.security.saml2.relyingparty.registration.{registrationId}.entity-id=
# spring.security.saml2.relyingparty.registration.{registrationId}.assertingparty.metadata-uri=
#### saml attibutes keys for firstname & lastname
# openaev.provider.{registrationId}.firstname_attribute_key=
# openaev.provider.{registrationId}.lastname_attribute_key=

## Oauth & Saml2
### To handle admin with sso
# openaev.provider.{registrationId}.roles_path=
# openaev.provider.{registrationId}.roles_admin=
# openaev.provider.{registrationId}.audience=
# openaev.provider.{registrationId}.groups_management=
# openaev.provider.{registrationId}.tenant_id=

## Kerberos
openaev.auth-kerberos-enable=false

# Technical
server.forward-headers-strategy=framework
spring.servlet.multipart.max-file-size=5GB
spring.servlet.multipart.max-request-size=5GB
spring.application.name=${openaev.name}
spring.quartz.properties.org.quartz.scheduler.instanceName=OpenAEV_Scheduler
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.quartz.properties.org.quartz.threadPool.threadCount=10
spring.data.rest.basePath=/api
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.validatorUrl=none
# Fixes incompatibiliy between spring boot >=3.5.7 and springdoc-api <= 2.8.15
# https://github.com/springdoc/springdoc-openapi/issues/3041
springdoc.override-with-generic-response=false

# Encoding
spring.mail.default-encoding=UTF-8
spring.messages.encoding=UTF-8
server.tomcat.uri-encoding=UTF-8
spring.freemarker.charset=UTF-8
server.servlet.encoding.charset=UTF-8

# ORM
spring.jpa.hibernate.ddl-auto=none


# Database schema migrations
spring.flyway.url=${spring.datasource.url}
spring.flyway.user=${spring.datasource.username}
spring.flyway.password=${spring.datasource.password}
spring.flyway.table=migrations
spring.flyway.locations=classpath:io/openaev/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.postgresql.transactional-lock=false
# optionally enable out of order processing.
# this allows for easier development by avoiding migrations clashes
# https://github.com/OpenAEV-Platform/openaev/issues/5281
spring.flyway.out-of-order=${OPENAEV_FLYWAY_OUT_OF_ORDER:false}
# Ignore applied migrations that are no longer resolved locally (e.g. the 20260720
# migrations renumbered in #6808): instances that already ran the old version numbers
# must not fail validation on boot. The renumbered replacements are idempotent.
spring.flyway.ignore-migration-patterns=${OPENAEV_FLYWAY_IGNORE_MIGRATION_PATTERNS:*:missing}

# AI Properties
ai.enabled=false
ai.type=mistralai
ai.endpoint=
ai.token=
ai.model=mistral
ai.model_images=

# Inject execution threshold, by default it's 10minutes
inject.execution.threshold.minutes=10

# Agent queue threshold: max number of pending jobs per agent before marking it overloaded.
# 0 disables the check. Configurable via env var OPENAEV_AGENT_QUEUE_THRESHOLD.
openaev.agent.queue-threshold=0

# Logging
logging.level.root=fatal
logging.level.org.flywaydb=error
logging.level.io.openaev=error
logging.level.com.zaxxer.hikari=warn
# Debug mode emits on io.openaev.debug at INFO/WARN; the parent io.openaev is pinned to ERROR, so the
# debug subtree is raised explicitly. Harmless when debug mode is off (those classes emit nothing).
logging.level.io.openaev.debug=INFO

###############
# DEBUG MODE  #
###############
# Global, instance-wide debug mode. OFF by default. When off there is no datasource proxy on the
# query hot path, no parameter capture and no extra per-request allocation.
# Activate (ideally via env var OPENAEV_DEBUG_ENABLED=true) ONLY for diagnostics, never in steady
# production: a loud warning is logged repeatedly while it is on.
# See docs/docs/administration/debug-mode.md for the full reference and the writable volume needed
# in containers.
openaev.debug.enabled=false
# Production barrier: in production (no dev/test/ci profile) the mode refuses to start unless this
# separate override is also set. Turning openaev.debug.enabled on is not enough in production.
openaev.debug.allow-in-production=false
# Auto-disable the verbose tracing after this duration (0 = never). A full removal of the datasource
# proxy still needs a restart.
openaev.debug.auto-disable-after=0
# Writable directory for debug artifacts: JFR recordings and the rotated SQL log file.
# On a read-only container, point this at a mounted writable volume.
openaev.debug.output-dir=./logs/debug
# SQL statement logging (timing + masked parameters), written to a rotated file
# (openaev-debug-sql.log) in output-dir, off the console. Active only when debug mode is on.
openaev.debug.sql.enabled=true
# 0 logs every statement; set e.g. 50ms to only log slow ones.
openaev.debug.sql.slow-query-threshold=0ms
# JFR recording (bounded in size and time, written to output-dir).
openaev.debug.jfr.enabled=true
openaev.debug.jfr.max-size=100MB
openaev.debug.jfr.max-age=1h
openaev.debug.jfr.duration=10m
openaev.debug.jfr.settings=profile
# Masking of secrets and personal data. Keep enabled.
openaev.debug.masking.enabled=true
# Deny-by-default: mask every parameter value (keep only column name + type). Opt-in to reveal less.
openaev.debug.masking.mask-all-parameters=false
# Correlation ids: DebugTracingContextInitializer excludes the tracing auto-configuration unless debug
# mode is active, so no Tracer is created and no span/traceId is produced on any request, message or
# job path when off (management.tracing.enabled only gates export, not span creation). This sampling
# probability applies once tracing is active under debug mode.
management.tracing.sampling.probability=1.0
logging.file.name=./logs/openaev.log
logging.logback.rollingpolicy.file-name-pattern=${LOG_FILE}.-%d{yyyy-MM-dd}.%i
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=7

################
# AUDIT LOGGING #
################
# Active transports (comma-separated): console, file, engine
# Audit logging is enabled when at least one transport is configured; leave empty to disable.
openaev.audit-logs.transports=
openaev.audit-logs.halt-on-failure=false

# File transport configuration
openaev.audit-logs.file.dir=logs
openaev.audit-logs.file.filename=audit
openaev.audit-logs.file.retention-days=365
openaev.audit-logs.file.rollover-max-size=100MB
openaev.audit-logs.file.rollover-total-size-cap=20GB

# Engine (Elasticsearch) transport configuration
# TODO(#6587): Re-enable and revisit these defaults when audit log engine transport is implemented.
# https://github.com/OpenAEV-Platform/openaev/issues/6587
# openaev.audit-logs.engine.retention-days=365
# openaev.audit-logs.engine.rollover-max-size=5gb
# openaev.audit-logs.engine.rollover-max-age=30d

#############
# EXECUTORS #
#############

# Executor Caldera
executor.caldera.enable=false
executor.caldera.id=2f9a0936-c327-4e95-b406-d161d32a2500
executor.caldera.url=<caldera-url>
executor.caldera.public-url=<caldera-public-url>
executor.caldera.api-key=<caldera-api-key>

# Executor Tanium
executor.tanium.enable=false
executor.tanium.id=fbcce3cc-9cd3-4fdb-b451-1c90f4ae035d
executor.tanium.url=<tanium-url>
executor.tanium.api-batch-execution-action-pagination=100
executor.tanium.api-register-interval=1200
executor.tanium.clean-implant-interval=8
executor.tanium.api-key=<tanium-api-key>
executor.tanium.computer-group-id=1
executor.tanium.action-group-id=4
executor.tanium.windows-package-id=
executor.tanium.unix-package-id=

# Executor CrowdStrike
executor.crowdstrike.enable=false
executor.crowdstrike.id=2a16dcc4-55ac-40fc-8110-d5968a46cdd1
executor.crowdstrike.api-url=https://api.us-2.crowdstrike.com
executor.crowdstrike.api-batch-execution-action-pagination=2500
executor.crowdstrike.api-register-interval=1200
executor.crowdstrike.clean-implant-interval=8
executor.crowdstrike.client-id=<crowdstrike-client-id>
executor.crowdstrike.client-secret=<crowdstrike-client-secret>
executor.crowdstrike.host-group=<host-group>
executor.crowdstrike.windows-script-name=OpenAEV Subprocessor (Windows)
executor.crowdstrike.unix-script-name=OpenAEV Subprocessor (Unix)

# Executor SentinelOne
executor.sentinelone.enable=false
executor.sentinelone.id=9a12c0dd-1f6d-4ed4-a773-8034934fc111
executor.sentinelone.url=<sentinelone-url>
executor.sentinelone.api-key=<sentinelone-api-key>
executor.sentinelone.api-batch-execution-action-pagination=2500
executor.sentinelone.api-register-interval=1200
executor.sentinelone.clean-implant-interval=8
executor.sentinelone.account-id=
executor.sentinelone.site-id=
executor.sentinelone.group-id=
executor.sentinelone.windows-script-id=
executor.sentinelone.unix-script-id=

# Executor OpenAEV

# Common defaults (apply to both agent and implant unless overridden below)
# Valid values: local | repository
executor.openaev.binaries.origin=local
# if executor.openaev.agent.binaries.origin is set to "local",
# this config is ignored
# default: the OpenAEV instance's version
executor.openaev.binaries.version=@project.version@

# Agent-specific overrides
#executor.openaev-agent.binaries.origin=repository
#executor.openaev-agent.binaries.version=@project.version@

# Implant-specific overrides
#executor.openaev-implant.binaries.origin=local
#executor.openaev-implant.binaries.version=@project.version@

# Simultaneous jobs execution limitation for agents, to avoid overloading the system.
executor.openaev.agent.max-simultaneous-jobs=5
# When disabled, the server will not enqueue agent upgrade jobs (global kill switch).
executor.openaev.agent.auto-update-enabled=true

#############
# INJECTORS #
#############

### /!\ MAKE SURE TO SET UP RABBITMQ (see RabbitMQ section above)

# Mail sending config (Always available, mandatory)
openaev.default-mailer=no-reply@openaev.io
openaev.default-mailer-name=no-reply
openaev.default-reply-to=contact@openaev.io
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=<username@mail.com>
spring.mail.password=<password>
# Extra mail configuration
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.starttls.enable=true
# IMAP Configuration
openaev.mail.imap.enabled=false
openaev.mail.imap.host=imap.mail.com
openaev.mail.imap.username=<username@mail.com>
openaev.mail.imap.password=<password>
openaev.mail.imap.port=993
openaev.mail.imap.inbox=INBOX
openaev.mail.imap.sent=Sent
# Extra IMAP configuration
openaev.mail.imap.ssl.trust=*
openaev.mail.imap.ssl.enable=true
openaev.mail.imap.auth=true
openaev.mail.imap.starttls.enable=false
# Extra XTM configuration
openaev.xtm.opencti.2cffad3a-0001-4078-b0e2-ef74274022c3.enable=false
openaev.xtm.opencti.2cffad3a-0001-4078-b0e2-ef74274022c3.url=<opencti-url>
#openaev.xtm.opencti.2cffad3a-0001-4078-b0e2-ef74274022c3.api_url=<opencti-api-url>
openaev.xtm.opencti.2cffad3a-0001-4078-b0e2-ef74274022c3.token=<opencti-token>
openaev.xtm.hub.enable=true
openaev.xtm.hub.url=https://hub.filigran.io
openaev.xtm.hub.connectivity-email-enable=true

#openaev.xtm.hub.override-api-url=<override-hub-api-url>

# XTM One (set url + token to enable registration)
#openaev.xtm.one.url=http://localhost:8000
#openaev.xtm.one.token=xtm-default-registration-token
openaev.xtm.hub.collector.enable=false
openaev.xtm.hub.collector.id=b402f1f5-29ba-4ee3-b366-f0467754cf4e
#openaev.xtm.hub.collector.connectivity-check-interval=3600000


# XLS Import
openaev.xls.import.mail.enable=true
openaev.xls.import.sms.enable=true

# Injector Caldera config
injector.caldera.enable=false
injector.caldera.id=7736918d-6a3f-46c7-b303-cbf5dc476c84
injector.caldera.url=<caldera-url>
injector.caldera.public-url=<caldera-public-url>
injector.caldera.api-key=<caldera-api-key>

# OVH SMS config
ovh.sms.enable=false
ovh.sms.ak=<ak>
ovh.sms.as=<as>
ovh.sms.ck=<ck>
ovh.sms.service=<service>
ovh.sms.sender=<sender>

# Mastodon config
mastodon.enable=false
mastodon.url=<url>

# Airbus LADE config
lade.enable=false
lade.url=<url>
lade.session=30
lade.username=<username>
lade.password=<password>

### Expectation

openaev.expectation.technical.expiration-time=21600
#openaev.expectation.detection.expiration-time=21600
# Delay in seconds before processing the SIMULATION_COMPLETED notification event (default: 3600 = 1h, allows expectations to be scored first)
openaev.notification.simulation-completed-delay-seconds=3600

openaev.expectation.human.expiration-time=86400
#openaev.expectation.challenge.expiration-time=3600
#openaev.expectation.article.expiration-time=3600
#openaev.expectation.manual.expiration-time=3600

# Min value: 1
# Max value: 100
# Default value: 50
openaev.expectation.manual.default-score-value=50


#Health check
# a unique random string used when calling the health endpoint
# Example of a call : /api/health?health_access_key=ChangeMe
openaev.healthcheck.key=ChangeMe

# Pyroscope properties
pyroscope.agent.enabled=false
pyroscope.agent.instance=OpenAEV
pyroscope.profiler.event=itimer
pyroscope.profiler.alloc=512k
pyroscope.profiler.lock=10ms
pyroscope.server.address=<pyroscope.url>
pyroscope.profile.export.timeout=300s

## remediation detection AI Webservice
#remediation.detection.webservice=
#remediation.detection.webservice.retry=
#remediation.detection.webservice.retry.waiting.milliseconds=


# Enable actuator -> disabled by default
# once activated the api is by default open without authentication
management.endpoints.web.exposure.include=
#management.endpoints.web.exposure.include=metrics,prometheus
management.endpoint.health.show-details=always
management.server.port=8080

# URL Access Token configuration
openaev.url.access.token.expiry-margin-days=7
openaev.url.access.token.retention-days=30

# Execution trace retention (high-growth table: one row per agent execution message).
# Number of days to keep execution traces; 0 (default) disables the purge job.
openaev.retention.execution-traces.retention-days=0

# Credential status validation: re-checks stored credentials against their provider and records
# ACTIVE/INACTIVE on the secret reference. An inconclusive check (timeout, throttling, provider
# outage) never overwrites the previous status.
openaev.credentials.status-validation.enabled=true
# Every 6 hours (by default) a run makes one outbound call per stale credential, keep it off-peak.
openaev.credentials.status-validation.cron=0 */6 * * * ?
# How long a status stays fresh before the credential is re-checked.
# If the value is negative, the value is set to default (1 day).
openaev.credentials.status-validation.revalidate-after-days=1
# Upper bound of credentials checked per tenant and per run.
openaev.credentials.status-validation.max-per-run=500
# Per-call budget for a single provider probe.
openaev.credentials.status-validation.timeout-seconds=10

#############################
# FEATURE UNDER DEVELOPMENT #
#############################
openaev.enabled-dev-features=

# Maximum number of records fetched per indexing batch from the database
# engine.indexing-batch-size=500

#############################
# Multi-tenancy v2
#############################
# Tables fully switched to v2 tenant isolation (statement inspector + can_access_tenant).
# import_mappers is the pilot: its v1 @Filter has been removed, so it MUST stay activated here,
# otherwise the table would have no tenant isolation at all.
# attackpath_execution / attackpath_finding are v2-native (no v1 @Filter), so they MUST stay
# activated here for the POC to have tenant isolation (attackpath_execution_finding is a join
# table, filtered through the finding it is joined to).
# autonomous_runs / autonomous_events / autonomous_directives are v2-native (TenantBase, no
# @Filter; TenantBaseListener removed on activation) so they MUST stay here (#7396).
openaev.tenant.active-tables=import_mappers,lessons_templates,mitigations,cwes,collectors,executors,injectors,attackpath_execution,attackpath_finding,secret_references,secrets,connector_instances,autonomous_runs,autonomous_events,autonomous_directives,kill_chain_phases,security_coverages

#############################
# Attack path
#############################
# Above this many executions a simulation's graph is served collapsed by default. The bound is the
# front's render ceiling, not backend latency: a full graph larger than this is not renderable.
openaev.attackpath.collapse-threshold=20000
# Beyond this many changed rows, a delta poll is answered with a resync instead: past that size
# assembling the delta costs more than a reload, which the client would do anyway to re-lay out.
openaev.attackpath.delta-max-rows=5000

#############################
# Chaining engine
#############################
openaev.chaining.max-retry-count=5
