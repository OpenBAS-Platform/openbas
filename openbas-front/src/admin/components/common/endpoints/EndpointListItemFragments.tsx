export enum EndpointListItemFragments {
    ASSET_NAME = "asset_name",
    ASSET_PLATFORM = "asset_platform",
    ASSET_TAGS = "asset_tags",
    ASSET_TYPE = "asset_type",

}

export const getFragment = (fragment_id: string) => {
    switch (frament_id) {
        case
    }
}